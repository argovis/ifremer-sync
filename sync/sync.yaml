apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: ifremer-mirror
spec:
  schedule: "0 2 9 4 *"
  jobTemplate:
    spec:
      template:
        metadata:
          name: ifremer-mirror
        spec:
          volumes:
            - name: ifremer-mirror
              persistentVolumeClaim:
                claimName: ifremer-mirror
            - name: logs
              persistentVolumeClaim:
                claimName: logs
          containers:
          - name: sync
            imagePullPolicy: Always
            image: argovis/ifremer-sync:dev
            volumeMounts:
              - mountPath: "/ifremer"
                name: ifremer-mirror
              - mountPath: "/logs"
                name: logs
            resources:
              requests:
                memory: "0Gi"
                cpu: "0m"
              limits:
                memory: 1.5Gi
                cpu: 500m
          restartPolicy: Never
          securityContext:
            runAsUser: 1000660000
            runAsGroup: 1000660000
      backoffLimit: 0
